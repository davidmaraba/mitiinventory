<?xml version="1.0" encoding="utf-8"?>

<com.mitiinventory.DashBoardLayout
   xmlns:android="http://schemas.android.com/apk/res/android"
   android:layout_width="fill_parent"
   android:layout_height="fill_parent"
   android:layout_weight="1"
  
     >

    <!-- Adding to inventory-->
    <Button 
        android:id="@+id/btn_addInventory"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/add_inventory"
        android:text="Add Inv"/>
    
    <!-- View remaining in inventory -->
    <Button 
        android:id="@+id/btn_checkRemaining"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/view_inventory"
        android:text="Check Inv"/>
     <!-- Points Button -->
    <Button 
        android:id="@+id/btn_deleteInventory"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/delete_inventory"
        android:text="Delete Inv"
        />
    <!-- Reorder Level -->
    <Button 
        android:id="@+id/btn_reorderLevel"
        style="@style/DashboardButton"
        android:drawableTop="@drawable/reorder_level"
        android:text="Reorder Level"/>
   
    <!-- About Button -->
    <Button 
        android:id="@+id/btn_About"
        style="@style/DashboardButton"
        />
        <Button 
        android:id="@+id/btn_logout"
        style="@style/DashboardButton"
      
        android:text=""/>
        
       
</com.mitiinventory.DashBoardLayout>